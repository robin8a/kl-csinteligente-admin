<h1>Editing Report</h1>

<%= render 'form', report: @report, activity: @activity, project: @project, user: @user %>

<%= link_to 'Show', user_project_activity_report_path(params[:user_id], params[:project_id], params[:activity_id], @report) %> |
<%= link_to 'Back', user_project_activity_reports_path %>
