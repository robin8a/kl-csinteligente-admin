<%= form_with(model: measure, local: true) do |form| %>
  <% if measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measure.errors.count, "error") %> prohibited this measure from being saved:</h2>

      <ul>
      <% measure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :me_name %>
    <%= form.text_field :me_name %>
  </div>

  <div class="field">
    <%= form.label :me_unit %>
    <%= form.text_field :me_unit %>
  </div>

  <div class="field">
    <%= form.label :me_is_active %>
    <%= form.check_box :me_is_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
